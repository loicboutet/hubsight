<% content_for :header, "Éditer Organisation" %>

<div class="section">
  <div style="margin-bottom: var(--space-xl);">
    <h2 class="typo-h2" style="margin-bottom: var(--space-sm);">Éditer l'Organisation</h2>
    <p class="typo-body-small">Modifier les informations de <%= @organization.name %></p>
  </div>

  <%= render 'form', organization: @organization %>
</div>
