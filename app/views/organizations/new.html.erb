<% content_for :header, "Nouvelle Organisation" %>

<div class="section">
  <div class="page-header-responsive" style="margin-bottom: var(--space-lg);">
    <div>
      <h2 class="typo-h2" style="margin-bottom: var(--space-sm);">Créer une nouvelle organisation</h2>
      <p class="typo-body-small">Ajouter une organisation (entreprise, prestataire, fournisseur...)</p>
    </div>
    <%= link_to organizations_path, class: "typo-btn-cancel" do %>
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
      </svg>
      Retour à la liste
    <% end %>
  </div>

  <%= render 'form' %>
</div>
