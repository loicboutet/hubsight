<% if impersonating? %>
  <div class="fixed top-0 left-0 right-0 z-50 bg-red-600">
    <div class="mx-auto max-w-7xl px-3 py-3 sm:px-6 lg:px-8">
      <div class="flex flex-wrap items-center justify-between">
        <div class="flex w-0 flex-1 items-center">
          <span class="flex rounded-lg bg-red-800 p-2">
            <svg class="h-6 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" />
            </svg>
          </span>
          <p class="ml-3 truncate font-medium text-white">
            <span class="md:hidden">Mode Admin: <%= impersonated_organization.name %></span>
            <span class="hidden md:inline">
              🔒 ACCÈS ADMINISTRATEUR - Vous visualisez les données de <strong><%= impersonated_organization.name %></strong>. Toutes vos actions sont enregistrées.
            </span>
          </p>
        </div>
        <div class="order-3 mt-2 w-full flex-shrink-0 sm:order-2 sm:mt-0 sm:w-auto">
          <%= button_to "Terminer l'accès", 
              admin_stop_impersonation_path, 
              method: :post,
              data: { turbo_confirm: "Êtes-vous sûr de vouloir terminer l'accès aux données du client?" },
              class: "flex items-center justify-center rounded-md border border-transparent bg-white px-4 py-2 text-sm font-medium text-red-600 shadow-sm hover:bg-red-50" %>
        </div>
        <div class="order-2 flex-shrink-0 sm:order-3 sm:ml-3">
          <button type="button" class="-mr-1 flex rounded-md p-2 hover:bg-red-500 focus:outline-none focus:ring-2 focus:ring-white sm:-mr-2">
            <span class="sr-only">Dismiss</span>
          </button>
        </div>
      </div>
    </div>
  </div>
  <!-- Spacer to push content down when banner is visible -->
  <div class="h-16"></div>
<% end %>
