<% content_for :header, "Tableau de Bord" %>
<%= stylesheet_link_tag "dashboard", "data-turbo-track": "reload" %>

<div class="section">
  <!-- KPI Cards -->
  <div class="dashboard-kpi-grid">
    <!-- Card 1: Sites Total -->
    <div class="dashboard-kpi-card">
      <div class="dashboard-kpi-content">
        <div class="dashboard-kpi-label">Sites Total</div>
        <div class="dashboard-kpi-value"><%= @sites_count || 0 %></div>
        <div class="dashboard-kpi-subtitle">Sites immobiliers</div>
      </div>
      <div class="dashboard-kpi-icon-container">
        <div class="dashboard-kpi-icon blue">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
          </svg>
        </div>
      </div>
    </div>

    <!-- Card 2: Bâtiments Total -->
    <div class="dashboard-kpi-card">
      <div class="dashboard-kpi-content">
        <div class="dashboard-kpi-label">Bâtiments Total</div>
        <div class="dashboard-kpi-value"><%= @buildings_count || 0 %></div>
        <div class="dashboard-kpi-subtitle"><% if @sites_count > 0 %><%= (@buildings_count.to_f / @sites_count).round(1) %> bâtiments/site en moyenne<% else %>Aucun site<% end %></div>
      </div>
      <div class="dashboard-kpi-icon-container">
        <div class="dashboard-kpi-icon pink">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
          </svg>
        </div>
      </div>
    </div>

    <!-- Card 3: Contrats Actifs -->
    <div class="dashboard-kpi-card">
      <div class="dashboard-kpi-content">
        <div class="dashboard-kpi-label">Contrats Actifs</div>
        <div class="dashboard-kpi-value"><%= @contracts_by_status['active'] || 0 %></div>
        <div class="dashboard-kpi-subtitle"><% if @total_contracts > 0 %><%= ((@contracts_by_status['active'] || 0) * 100.0 / @total_contracts).round %>% de portfolio couvert<% else %>Aucun contrat<% end %></div>
      </div>
      <div class="dashboard-kpi-icon-container">
        <div class="dashboard-kpi-icon red">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
          </svg>
        </div>
      </div>
    </div>

    <!-- Card 4: Prestataires -->
    <div class="dashboard-kpi-card complex">
      <div class="dashboard-kpi-content">
        <div class="dashboard-kpi-label">PRESTATAIRES</div>
        <div class="dashboard-kpi-value"><%= @providers_count || 0 %></div>
        <div class="dashboard-kpi-details">
          <div>Organisations actives</div>
          <% if @avg_contracts_per_provider %>
            <div>Moy. contrats/prestataire: <strong><%= @avg_contracts_per_provider %></strong></div>
          <% end %>
          <% if @top_provider_name %>
            <div>Top prestataire: <strong><%= @top_provider_name %></strong> (<%= @top_provider_count %> <%= @top_provider_count == 1 ? 'contrat' : 'contrats' %>)</div>
          <% end %>
        </div>
      </div>
      <div class="dashboard-kpi-icon-container">
        <div class="dashboard-kpi-icon orange">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
          </svg>
        </div>
      </div>
    </div>

    

    <!-- Card 5: Budget Annuel Total -->
    <div class="dashboard-kpi-card">
      <div class="dashboard-kpi-content">
        <div class="dashboard-kpi-label">Budget Annuel Total</div>
        <div class="dashboard-kpi-value"><%= number_to_currency(@total_spending, unit: "", precision: 0, delimiter: " ") %> €</div>
        <div class="dashboard-kpi-subtitle"><%= pluralize(@total_contracts, 'contrat') %></div>
      </div>
      <div class="dashboard-kpi-icon-container">
        <div class="dashboard-kpi-icon purple">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
      </div>
    </div>

    <!-- Card 6: Économies Potentielles (Items 34 & 36) -->
    <!-- 
      BACKEND REQUIRED: This card requires the following variables from DashboardController:
      @contracts_count = Total number of contracts for current user/organization
      @savings_unlocked = Boolean: true if @contracts_count >= 5
      @contracts_needed = [5 - @contracts_count, 0].max
      @progress_percentage = (@contracts_count * 100 / 5).clamp(0, 100)
      
      For now, using placeholder values for demonstration:
    -->
    <% 
      # TODO: Remove these placeholder values once backend is implemented
      contracts_count = 2  # Replace with: @contracts_count
      savings_unlocked = false  # Replace with: @savings_unlocked
      contracts_needed = 3  # Replace with: @contracts_needed
      progress_percentage = 40  # Replace with: @progress_percentage
    %>
    
    <% if savings_unlocked %>
      <!-- UNLOCKED STATE: Show regular card with link to savings -->
      <%= link_to savings_path, style: "text-decoration: none; color: inherit;" do %>
        <div class="dashboard-kpi-card" style="cursor: pointer; transition: transform 0.2s ease;">
          <div class="dashboard-kpi-content">
            <div class="dashboard-kpi-label">Économies Potentielles</div>
            <div class="dashboard-kpi-value">127 350 €</div>
            <div class="dashboard-kpi-subtitle positive">4.5% sur 58 contrats</div>
          </div>
          <div class="dashboard-kpi-icon-container">
            <div class="dashboard-kpi-icon green">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
              </svg>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <!-- LOCKED STATE: Show grayed-out card with progress (Items 34 & 36) -->
      <div class="dashboard-kpi-card" style="opacity: 0.6; position: relative; cursor: not-allowed; background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);">
        <div class="dashboard-kpi-content">
          <div class="dashboard-kpi-label" style="color: #6c757d;">Économies Potentielles</div>
          <div class="dashboard-kpi-value" style="color: #adb5bd;">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="none" viewBox="0 0 24 24" stroke="#adb5bd" stroke-width="2" style="display: inline-block;">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
            </svg>
          </div>
          <div class="dashboard-kpi-subtitle" style="color: #6c757d; font-size: 0.75rem; margin-top: 8px;">
            <!-- Progress Bar (Item 36) -->
            <div style="margin-bottom: 8px;">
              <% 
                filled_blocks = contracts_count
                empty_blocks = 5 - contracts_count
              %>
              <div style="font-family: monospace; font-size: 1.2rem; letter-spacing: 2px;">
                <span style="color: #28a745;"><%= '█' * filled_blocks %></span><span style="color: #e0e0e0;"><%= '░' * empty_blocks %></span>
              </div>
            </div>
            <div style="font-weight: 600; color: #495057;">
              <%= contracts_count %>/5 contrats — <%= contracts_needed %> <%= contracts_needed == 1 ? 'de plus' : 'de plus' %> pour déverrouiller
            </div>
            <div style="margin-top: 4px; font-size: 0.7rem; color: #868e96;">
              l'analyse des économies
            </div>
          </div>
        </div>
        <div class="dashboard-kpi-icon-container">
          <div class="dashboard-kpi-icon" style="background-color: #adb5bd;">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
            </svg>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Card 7: Alertes Actives - Brick 2 Feature -->
    <div class="dashboard-kpi-card" style="opacity: 0.6; background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);">
      <div class="dashboard-kpi-content">
        <div class="dashboard-kpi-label" style="color: #6c757d;">Alertes Actives</div>
        <div class="dashboard-kpi-value" style="color: #adb5bd; font-size: 1.2rem;">
          🔔
        </div>
        <div class="dashboard-kpi-subtitle" style="color: #6c757d; font-size: 0.8rem;">
          Disponible dans Brique 2
        </div>
      </div>
      <div class="dashboard-kpi-icon-container">
        <div class="dashboard-kpi-icon" style="background-color: #adb5bd;">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
          </svg>
        </div>
      </div>
    </div>
  </div>

  <!-- KEY INDICATORS SECTION - ITEM 45 -->
  <div style="background-color: var(--white); border-radius: var(--space-md); padding: var(--space-xl); margin-bottom: var(--space-xl);">
    <h3 class="typo-h4" style="margin-bottom: var(--space-lg); padding-bottom: var(--space-md); border-bottom: 2px solid var(--light-gray);">
      📊 Indicateurs Clés
    </h3>
    
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: var(--space-lg);">
      <!-- Contracts by Status -->
      <div style="padding: var(--space-lg); background-color: #f8f9fa; border-radius: var(--space-md);">
        <h4 class="typo-h5" style="margin-bottom: var(--space-md); color: var(--dark-gray);">Contrats par Statut</h4>
        <div style="display: flex; flex-direction: column; gap: var(--space-sm);">
          <% 
            status_config = {
              'active' => { label: 'Actif', color: '#28a745' },
              'pending' => { label: 'En attente', color: '#ffc107' },
              'expired' => { label: 'Expiré', color: '#dc3545' },
              'suspended' => { label: 'Suspendu', color: '#6c757d' }
            }
            status_config.each do |status, config|
              count = @contracts_by_status[status] || 0
              percentage = @total_contracts > 0 ? (count * 100.0 / @total_contracts).round : 0
          %>
            <div style="display: flex; justify-content: space-between; align-items: center;">
              <span style="font-size: 0.875rem;"><%= config[:label] %></span>
              <div style="display: flex; align-items: center; gap: var(--space-sm);">
                <span style="font-weight: 600; color: <%= config[:color] %>;"><%= count %></span>
                <span style="font-size: 0.75rem; color: var(--medium-gray);">(<%= percentage %>%)</span>
              </div>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Upcoming Renewals -->
      <div style="padding: var(--space-lg); background-color: #f8f9fa; border-radius: var(--space-md);">
        <h4 class="typo-h5" style="margin-bottom: var(--space-md); color: var(--dark-gray);">Renouvellements à Venir</h4>
        <div style="display: flex; flex-direction: column; gap: var(--space-sm);">
          <div style="display: flex; justify-content: space-between; align-items: center;">
            <span style="font-size: 0.875rem;">30 prochains jours</span>
            <span style="font-weight: 600; padding: 2px 8px; border-radius: 4px; background-color: #dc3545; color: white; font-size: 0.875rem;"><%= @renewals_30_days %></span>
          </div>
          <div style="display: flex; justify-content: space-between; align-items: center;">
            <span style="font-size: 0.875rem;">31-60 jours</span>
            <span style="font-weight: 600; padding: 2px 8px; border-radius: 4px; background-color: #ffc107; color: white; font-size: 0.875rem;"><%= @renewals_60_days %></span>
          </div>
          <div style="display: flex; justify-content: space-between; align-items: center;">
            <span style="font-size: 0.875rem;">61-90 jours</span>
            <span style="font-weight: 600; padding: 2px 8px; border-radius: 4px; background-color: #17a2b8; color: white; font-size: 0.875rem;"><%= @renewals_90_days %></span>
          </div>
        </div>
      </div>

      <!-- Total Spend by Category -->
      <div style="padding: var(--space-lg); background-color: #f8f9fa; border-radius: var(--space-md);">
        <h4 class="typo-h5" style="margin-bottom: var(--space-md); color: var(--dark-gray);">Dépenses par Catégorie</h4>
        <div style="display: flex; flex-direction: column; gap: var(--space-sm);">
          <div style="padding: var(--space-sm); background-color: #e3f2fd; border-radius: var(--space-xs);">
            <div style="font-size: 0.75rem; color: var(--medium-gray); margin-bottom: 2px;">Total</div>
            <div style="font-weight: 700; font-size: 1.25rem; color: var(--dark-gray);"><%= number_to_currency(@total_spending, unit: "", precision: 0, delimiter: " ") %> €</div>
          </div>
          <% @family_distribution.first(3).each do |family| %>
            <div style="display: flex; justify-content: space-between; align-items: center;">
              <span style="font-size: 0.75rem; color: var(--dark-gray);"><%= family[:name] || 'Non défini' %></span>
              <span style="font-weight: 600; font-size: 0.875rem;"><%= number_to_currency(family[:amount], unit: "", precision: 0, delimiter: " ") %> €</span>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <!-- Distribution and Top Contracts Section -->
  <div style="display: grid; grid-template-columns: 1fr 2fr; gap: var(--space-lg); margin-bottom: var(--space-xl);">
    <!-- Purchase Family Distribution -->
    <div style="background-color: var(--white); border-radius: var(--space-md);">
      <h3 class="typo-h4" style="margin-bottom: var(--space-lg); padding-bottom: var(--space-md); border-bottom: 2px solid var(--light-gray);">
        Répartition par Famille d'Achats
      </h3>
      <div class="distribution-list">
        <% 
          family_colors = ['#667eea', '#e879f9', '#3b82f6', '#10b981', '#ec4899', '#f59e0b', '#06b6d4', '#8b5cf6', '#ef4444', '#14b8a6']
          @family_distribution.each_with_index do |family, index| 
        %>
          <div class="distribution-item">
            <div class="distribution-header">
              <span class="distribution-name"><%= family[:name] || 'Non défini' %></span>
              <span class="distribution-percentage"><%= family[:percentage] %>%</span>
            </div>
            <div class="distribution-bar-container">
              <div class="distribution-bar" style="width: <%= family[:percentage] %>%; background-color: <%= family_colors[index % family_colors.length] %>;"></div>
            </div>
            <div class="distribution-details">
              <span><%= family[:contracts] %> contrats • <%= number_to_currency(family[:amount], unit: "", precision: 0, delimiter: " ") %> €</span>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Top 10 Contracts by Value -->
    <div style="background-color: var(--white); border-radius: var(--space-md);">
      <h3 class="typo-h4" style="margin-bottom: var(--space-lg); padding-bottom: var(--space-md); border-bottom: 2px solid var(--light-gray);">
        Top 10 Contrats par Valeur
      </h3>
      <div class="contracts-table-wrapper">
        <% if @top_contracts.any? %>
          <table class="contracts-table">
            <thead>
              <tr>
                <th>N° CONTRAT</th>
                <th>TITRE</th>
                <th>PRESTATAIRE</th>
                <th>MONTANT HT</th>
                <th>SITE</th>
                <th>ÉCHÉANCE</th>
              </tr>
            </thead>
            <tbody>
              <% @top_contracts.each do |contract| %>
                <tr>
                  <td><span class="contract-number"><%= contract.contract_number %></span></td>
                  <td><%= contract.title %></td>
                  <td><%= contract.contractor_organization_name || '—' %></td>
                  <td class="amount">
                    <%= number_to_currency(contract.annual_amount, unit: "", precision: 0, delimiter: " ") %> €
                  </td>
                  <td><%= contract.site&.name || 'Tous sites' %></td>
                  <td><%= contract.end_date ? contract.end_date.strftime('%d/%m/%Y') : '—' %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        <% else %>
          <div style="padding: var(--space-xl); text-align: center; color: var(--medium-gray);">
            <p>Aucun contrat avec montant disponible</p>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Main Content Grid -->
  <div style="display: grid; grid-template-columns: 2fr 1fr; gap: var(--space-lg);">
    <!-- Recent Contracts -->
    <div style="background-color: var(--white); border-radius: var(--space-md);">
      <h3 class="typo-h4" style="margin-bottom: var(--space-lg); padding-bottom: var(--space-md); border-bottom: 2px solid var(--light-gray);">
        Contrats Récents
      </h3>
      <div class="contracts-table-wrapper">
        <% if @recent_contracts.any? %>
          <table class="contracts-table">
            <thead>
              <tr>
                <th>N° CONTRAT</th>
                <th>TITRE</th>
                <th>PRESTATAIRE</th>
                <th>MONTANT HT</th>
                <th>SITE</th>
                <th>DATE</th>
                <th>STATUT</th>
              </tr>
            </thead>
            <tbody>
              <% @recent_contracts.each do |contract| %>
                <tr>
                  <td><span class="contract-number"><%= contract.contract_number %></span></td>
                  <td><%= contract.title %></td>
                  <td><%= contract.contractor_organization_name || '—' %></td>
                  <td class="amount">
                    <%= contract.annual_amount ? number_to_currency(contract.annual_amount, unit: "", precision: 0, delimiter: " ") + " €" : '—' %>
                  </td>
                  <td><%= contract.site&.name || '—' %></td>
                  <td><%= contract.created_at.strftime('%d %b %Y') %></td>
                  <td>
                    <% 
                      status_styles = {
                        'active' => 'background-color: #d4edda; color: #155724;',
                        'pending' => 'background-color: #fff3cd; color: #856404;',
                        'expired' => 'background-color: #f8d7da; color: #721c24;',
                        'suspended' => 'background-color: #e2e8f0; color: #4a5568;'
                      }
                      status_labels = {
                        'active' => 'Actif',
                        'pending' => 'En attente',
                        'expired' => 'Expiré',
                        'suspended' => 'Suspendu'
                      }
                    %>
                    <span style="display: inline-block; padding: 4px 10px; border-radius: 12px; font-size: 0.75rem; font-weight: 600; <%= status_styles[contract.status] || status_styles['active'] %>">
                      <%= status_labels[contract.status] || contract.status || 'Actif' %>
                    </span>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        <% else %>
          <div style="padding: var(--space-xl); text-align: center; color: var(--medium-gray);">
            <p>Aucun contrat récent</p>
          </div>
        <% end %>
      </div>
      <div style="margin-top: var(--space-lg); text-align: right; padding-right: var(--space-md);">
        <%= link_to contracts_path, class: "typo-body-small", style: "color: var(--coral-primary); text-decoration: none; font-weight: 600;" do %>
          Voir tous les contrats →
        <% end %>
      </div>
    </div>

    <!-- Sidebar -->
    <div style="display: flex; flex-direction: column; gap: var(--space-lg);">
      <!-- Alerts - Brick 2 Feature -->
      <div style="background-color: var(--white); border-radius: var(--space-md); padding: var(--space-xl); opacity: 0.7;">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: var(--space-lg); padding-bottom: var(--space-md); border-bottom: 2px solid var(--light-gray);">
          <h3 class="typo-h4" style="margin: 0; color: #6c757d;">Alertes Actives</h3>
        </div>
        <div style="text-align: center; padding: var(--space-xl) var(--space-md);">
          <div style="font-size: 3rem; margin-bottom: var(--space-md);">🔔</div>
          <p style="color: #6c757d; font-weight: 600; margin-bottom: var(--space-sm);">Fonctionnalité à venir</p>
          <p style="color: #868e96; font-size: 0.875rem;">Les alertes seront disponibles dans Brique 2</p>
        </div>
      </div>

      <!-- Quick Actions (Hidden for Technicians) -->
      <% unless current_user&.technician? %>
        <%= render 'shared/quick_actions' %>
      <% end %>
    </div>
  </div>
</div>

<!-- Responsive CSS for mobile -->
<style>
  @media (max-width: 1024px) {
    div[style*="grid-template-columns: 2fr 1fr"] {
      grid-template-columns: 1fr !important;
    }
  }
</style>
