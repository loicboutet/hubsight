<% content_for :header, "Éditer Agence" %>

<div class="section">
  <!-- Breadcrumb Navigation -->
  <div style="margin-bottom: var(--space-xl);">
    <%= render 'shared/breadcrumb', path: [
      { name: 'Agences', url: agencies_path },
      { name: @agency.display_name, url: agency_path(@agency) },
      { name: 'Éditer', url: nil }
    ] %>
  </div>

  <!-- Header Section -->
  <div style="margin-bottom: var(--space-xl);">
    <div style="display: flex; align-items: center; gap: var(--space-md); margin-bottom: var(--space-sm);">
      <div style="font-size: 32px;">🏪</div>
      <h2 class="typo-h2">Éditer l'Agence</h2>
    </div>
    <p class="typo-body-small" style="color: var(--medium-gray);">
      Modifier les informations de <%= @agency.display_name %>
    </p>
  </div>

  <!-- Form -->
  <%= render 'form' %>
</div>
