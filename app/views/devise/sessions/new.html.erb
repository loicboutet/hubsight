<div style="text-align: center; margin-bottom: var(--space-2xl);">
  <%= link_to root_path, style: "display: inline-block; margin-bottom: var(--space-lg);" do %>
    <%= image_tag "hugsight_logo.png", alt: "HubSight Logo", style: "height: 60px; width: auto; display: block;" %>
  <% end %>
  <h2 class="typo-h2" style="margin-bottom: var(--space-sm);">Connexion</h2>
  <p class="typo-body-small typo-color-secondary">Connectez-vous à votre compte HubSight</p>
</div>

<div class="section" style="background-color: var(--white); border-radius: var(--space-md); padding: var(--space-xl); box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div style="margin-bottom: var(--space-lg);">
      <%= f.label :email, "Adresse Email", class: "typo-body-small typo-bold", style: "display: block; margin-bottom: var(--space-sm);" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "votre.email@example.com", style: "width: 100%; padding: 12px; border: 1px solid var(--light-gray); border-radius: var(--space-sm); font-size: 1rem;" %>
    </div>

    <div style="margin-bottom: var(--space-lg);">
      <%= f.label :password, "Mot de Passe", class: "typo-body-small typo-bold", style: "display: block; margin-bottom: var(--space-sm);" %>
      <%= f.password_field :password, autocomplete: "current-password", placeholder: "Entrez votre mot de passe", style: "width: 100%; padding: 12px; border: 1px solid var(--light-gray); border-radius: var(--space-sm); font-size: 1rem;" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div style="margin-bottom: var(--space-xl);">
        <label style="display: flex; align-items: center; gap: var(--space-sm); cursor: pointer;">
          <%= f.check_box :remember_me, style: "width: 18px; height: 18px; cursor: pointer;" %>
          <span class="typo-body-small">Se souvenir de moi</span>
        </label>
      </div>
    <% end %>

    <div style="margin-bottom: var(--space-lg);">
      <%= f.submit "Se Connecter", class: "typo-btn-success", style: "width: 100%; cursor: pointer; justify-content: center;" %>
    </div>

    <%= render "devise/shared/links" %>
  <% end %>
</div>
