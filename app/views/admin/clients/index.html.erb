<div class="px-4 sm:px-6 lg:px-8 py-10">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="text-3xl font-bold leading-tight tracking-tight text-gray-900">Accès aux Données Clients</h1>
      <p class="mt-2 text-sm text-gray-700">
        Liste des organisations clientes. Vous pouvez accéder aux données d'un client pour consultation ou support.
        <strong class="text-red-600">Toutes vos actions sont enregistrées et auditées.</strong>
      </p>
    </div>
  </div>

  <div class="mt-8 flow-root">
    <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
        <table class="min-w-full divide-y divide-gray-300">
          <thead>
            <tr>
              <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-0">
                Organisation
              </th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                Statut
              </th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                Portfolio Managers
              </th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                Site Managers
              </th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                Total Utilisateurs
              </th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                Date de création
              </th>
              <th scope="col" class="relative py-3.5 pl-3 pr-4 sm:pr-0">
                <span class="sr-only">Actions</span>
              </th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-200">
            <% if @organizations.empty? %>
              <tr>
                <td colspan="7" class="px-3 py-10 text-center text-sm text-gray-500">
                  Aucune organisation trouvée.
                </td>
              </tr>
            <% else %>
              <% @organizations.each do |organization| %>
                <tr class="hover:bg-gray-50">
                  <td class="whitespace-nowrap py-4 pl-4 pr-3 text-sm sm:pl-0">
                    <div class="flex items-center">
                      <div>
                        <div class="font-medium text-gray-900"><%= organization.name %></div>
                        <% if organization.legal_name.present? && organization.legal_name != organization.name %>
                          <div class="text-gray-500 text-xs"><%= organization.legal_name %></div>
                        <% end %>
                        <% if organization.siret.present? %>
                          <div class="text-gray-400 text-xs">SIRET: <%= organization.siret %></div>
                        <% end %>
                      </div>
                    </div>
                  </td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm">
                    <% if organization.active? %>
                      <span class="inline-flex items-center rounded-full bg-green-50 px-2 py-1 text-xs font-medium text-green-700 ring-1 ring-inset ring-green-600/20">
                        Actif
                      </span>
                    <% else %>
                      <span class="inline-flex items-center rounded-full bg-gray-50 px-2 py-1 text-xs font-medium text-gray-600 ring-1 ring-inset ring-gray-500/10">
                        Inactif
                      </span>
                    <% end %>
                  </td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                    <%= @organization_stats[organization.id][:portfolio_managers] %>
                  </td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                    <%= @organization_stats[organization.id][:site_managers] %>
                  </td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                    <%= @organization_stats[organization.id][:total_users] %>
                  </td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                    <%= l(organization.created_at, format: :short) %>
                  </td>
                  <td class="relative whitespace-nowrap py-4 pl-3 pr-4 text-right text-sm font-medium sm:pr-0">
                    <div class="flex justify-end gap-2">
                      <%= link_to admin_organization_path(organization), 
                          class: "text-indigo-600 hover:text-indigo-900" do %>
                        Voir
                      <% end %>
                      <%= button_to "Accéder aux données", 
                          impersonate_admin_client_path(organization), 
                          method: :post,
                          data: { 
                            turbo_confirm: "Êtes-vous sûr de vouloir accéder aux données de #{organization.name}? Cette action sera enregistrée dans les logs d'audit." 
                          },
                          class: "inline-flex items-center rounded-md bg-red-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-red-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-600" %>
                    </div>
                  </td>
                </tr>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>

  <div class="mt-8 rounded-md bg-yellow-50 p-4">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-yellow-400" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M8.485 2.495c.673-1.167 2.357-1.167 3.03 0l6.28 10.875c.673 1.167-.17 2.625-1.516 2.625H3.72c-1.347 0-2.189-1.458-1.515-2.625L8.485 2.495zM10 5a.75.75 0 01.75.75v3.5a.75.75 0 01-1.5 0v-3.5A.75.75 0 0110 5zm0 9a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd" />
        </svg>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-yellow-800">Attention - Accès aux données client</h3>
        <div class="mt-2 text-sm text-yellow-700">
          <ul class="list-disc pl-5 space-y-1">
            <li>L'accès aux données client doit être justifié par un besoin légitime (support, maintenance, assistance)</li>
            <li>Toutes vos actions sont enregistrées avec horodatage et adresse IP</li>
            <li>Les sessions d'accès expirent automatiquement après 2 heures</li>
            <li>N'accédez aux données que si vous êtes autorisé contractuellement</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
