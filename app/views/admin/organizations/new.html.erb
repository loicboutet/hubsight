<% content_for :header, "Nouvelle Organisation" %>

<div class="section">
  <div style="margin-bottom: var(--space-xl);">
    <a href="<%= admin_organizations_path %>" style="color: var(--coral-primary); text-decoration: none; display: inline-flex; align-items: center; gap: var(--space-sm); margin-bottom: var(--space-lg);">
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
      </svg>
      Retour à la liste
    </a>
    
    <h2 class="typo-h2" style="margin-bottom: var(--space-sm);">Créer une Nouvelle Organisation</h2>
    <p class="typo-body-small">Ajouter une nouvelle organisation cliente au système</p>
  </div>

  <%= render 'form' %>
</div>
